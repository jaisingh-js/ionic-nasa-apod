import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter01',
  appName: 'ionic-nasa-apod',
  webDir: 'docs',
  bundledWebRuntime: false
};

export default config;
